server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 10MB     # ?? ? ??
      max-request-size: 10MB  # ?? ?(??? ?????)
  web:
    resources:
      static-locations: file:///C:/uploadFiles/