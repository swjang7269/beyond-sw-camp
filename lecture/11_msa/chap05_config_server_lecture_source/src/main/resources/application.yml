server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:\beyond-sw-camp\lecture\11_msa\config # ??? ?? ?? ?? -> git? ???  ??? ? ??? ?